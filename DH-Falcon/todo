

2) Initial header should make partition and then  do reading from file.
2.a if (rank==0) store partition file in nfs mounted folder.
2.c then read the partition file.

Tomorrow
---------
Code generation for single statement.


Then left DMR code generation.

Errors
-----
TARGET==2 for loop on relaxgraph called for local+remote points
